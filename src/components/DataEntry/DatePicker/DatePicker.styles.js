import { css } from 'linaria';
import { red } from '../../../constants/colors';

export const required = css`
  color: ${red};
  position: relative;
  right: 3px;
`;

export const growInput = css`
  display: block;
  margin: 6px 0;
`;
